node_modules/
dist/
build/

# Environment / secrets — never commit real values
.env
.env.*
!.env.example

# Local dev persistence (JSON store, uploaded images, dev session secret)
.data/

# Vercel
.vercel/

.DS_Store
Thumbs.db
*.log
